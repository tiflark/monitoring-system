Контекст проекта:

Цель проекта Создать портфолио-проект для демонстрации навыков в C++, системном программировании и DevOps — распределенную систему мониторинга с кросс-платформенными агентами на C++.

**Что уже сделано:**
1. ✅ Развернута инфраструктура на Ubuntu Server (InfluxDB 2.7 + Grafana в Docker)
2. ✅ Написан C++ агент для сбора метрик (CPU, память) на Linux
3. ✅ Реализован Python сервер на FastAPI для приема метрик
4. ✅ Настроена интеграция: агент → API → InfluxDB → Grafana
5. ✅ Создана структура Git репозитория с тестами и CI/CD

---

Технологический стек

Backend (C++):
- Язык: C++17 с использованием RAII, умных указателей, STL
- Сборка: CMake 3.15+, кросс-платформенная сборка
- Библиотеки: libcurl для HTTP, nlohmann/json для JSON (опционально)
- Тестирование: Google Test для unit-тестов

Backend (Python):
- Фреймворк: FastAPI с асинхронной поддержкой
- База данных: InfluxDB 2.7 (TSDB) через influxdb-client
- Валидация: Pydantic модели

DevOps инфраструктура:
- Контейнеризация: Docker + Docker Compose
- Мониторинг: InfluxDB (хранение) + Grafana (визуализация)
- CI/CD: GitHub Actions (сборка, тестирование, деплой)
- Виртуализация: Ubuntu Server с доступом по SSH

---

Архитектура проекта:

Структура файлов и папок:

monitoring-system/
├── 📁 agent/                    # C++ агент для сбора метрик
│   ├── 📁 src/                 # Исходный код агента
│   │   ├── main.cpp           # Точка входа, основной цикл
│   │   ├── system_info.h/cpp  # Сбор системных метрик (Linux/macOS)
│   │   └── http_client.h/cpp  # HTTP клиент для отправки данных
│   ├── 📁 tests/              # Unit-тесты (Google Test)
│   │   ├── test_system_info.cpp
│   │   └── test_http_client.cpp
│   ├── CMakeLists.txt         # Конфигурация сборки CMake
│   └── 📁 build/              # Собранные бинарники (не в git)
│
├── 📁 receiver/               # Python сервер приема метрик
│   ├── 📁 src/
│   │   └── server.py         # FastAPI приложение с эндпоинтами
│   ├── requirements.txt      # Зависимости Python
│   ├── run.sh               # Скрипт запуска
│   └── 📁 venv/             # Виртуальное окружение (не в git)
│
├── 📁 docker/                # Docker инфраструктура
│   ├── docker-compose.yml    # Запуск InfluxDB + Grafana
│   └── 📁 grafana/
│       └── provisioning/     # Автоматическая настройка Grafana
│
├── 📁 integration_tests/     # Интеграционные тесты
│   └── test_full_system.py  # Python тест всей системы
│
├── 📁 docs/                  # Документация
│   ├── architecture.md      # Описание архитектуры
│   └── images/             # Скриншоты dashboard
│
├── 📁 .github/workflows/    # CI/CD пайплайны
│   └── test.yml            # GitHub Actions для тестов
│
├── Makefile                # Управление сборкой и тестами
├── README.md              # Основная документация
├── .gitignore             # Игнорируемые файлы
└── LICENSE                # MIT лицензия


Архитектурная схема
┌─────────────┐     HTTP/JSON     ┌─────────────┐     InfluxDB     ┌─────────────┐
│   C++ Agent │───────────────────▶│  FastAPI    │─────────────────▶│   InfluxDB  │
│  (Linux/mac)│  :8000/api/metrics│   Receiver  │    :8086/write   │     TSDB    │
└─────────────┘                   └─────────────┘                  └──────┬──────┘
                                                                           │
                                                                   ┌──────▼──────┐
                                                                   │   Grafana   │
                                                                   │  Dashboard  │
                                                                   └─────────────┘



Текущее состояние системы:

Что работает прямо сейчас
1. Агент (C++): Собирает базовые метрики (CPU, память, hostname) на Linux
2. Сервер (Python): Принимает POST-запросы с метриками, валидирует, пишет в InfluxDB
3. Инфраструктура: InfluxDB + Grafana в Docker контейнерах
4. Визуализация: Grafana дашборд с метриками в реальном времени
5. Автоматизация: Makefile для управления, Docker Compose для развертывания

Команды для запуска:
bash ->
# 1. Запуск инфраструктуры
cd docker && docker-compose up -d

# 2. Запуск сервера
cd receiver && ./run.sh

# 3. Сборка и запуск агента
cd agent && mkdir build && cd build && cmake .. && make
./agent http://localhost:8000/metrics 10
